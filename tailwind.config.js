// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        // will be available as `font-sans`
        sans: ['Inter', 'ui-sans-serif', 'system-ui'],
        // will be available as `font-mono`
        mono: ['JetBrains Mono', 'ui-monospace', 'SFMono-Regular'],
      },
    },
  },
  plugins: [],
}
